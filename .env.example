# AWS Region where Amazon Bedrock and the selected model are available.
AWS_REGION=us-east-1

# Override with a model available in your AWS account/Region.
BEDROCK_MODEL_ID=amazon.nova-micro-v1:0

# Optional inference defaults.
BEDROCK_MAX_TOKENS=512
BEDROCK_TEMPERATURE=0.4
BEDROCK_TOP_P=0.9
